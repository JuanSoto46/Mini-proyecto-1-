# Vite exposes variables prefixed with VITE_
# Example API base URL for your server (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000/api
# Optional: set a brand/app name for the header
VITE_APP_NAME=Taskly
